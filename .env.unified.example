# ======================================================================
# Unified Homelab Environment Configuration
# ======================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.unified.example .env
#   nano .env
# ======================================================================
# See docs/ENV_VARIABLE_MATRIX.md for complete variable reference
# ======================================================================

# ============================================
# General / Shared Configuration
# ============================================
SERVICE_USER=evin
LETSENCRYPT_EMAIL=admin@evindrake.net

# Shared OpenAI API (fallback for all services)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# ============================================
# Homelab Dashboard (host.evindrake.net)
# ============================================
SESSION_SECRET=generate-with-python-secrets
DASHBOARD_API_KEY=generate-with-python-secrets

# Web Login (optional - defaults shown)
WEB_USERNAME=evin
WEB_PASSWORD=homelab

# Dashboard-specific OpenAI override (optional - uses OPENAI_API_KEY if empty)
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock

# SSH Configuration (for remote script execution)
SSH_HOST=localhost
SSH_PORT=22
SSH_USER=evin
SSH_KEY_PATH=/home/evin/.ssh/id_rsa
ENABLE_SCRIPT_EXECUTION=true

# ============================================
# Discord Ticket Bot
# ============================================
# PostgreSQL Database
DISCORD_DB_PASSWORD=generate-random-password

# Discord Application (from Discord Developer Portal)
# https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your-bot-token-here
DISCORD_CLIENT_ID=your-client-id
DISCORD_CLIENT_SECRET=your-client-secret
DISCORD_APP_ID=your-app-id
VITE_DISCORD_CLIENT_ID=your-client-id

# OAuth Configuration
DISCORD_SESSION_SECRET=generate-random-secret

# WebSocket URL (optional)
VITE_CUSTOM_WS_URL=

# Database Reset (set to 'true' to reset database on startup)
RESET_DB=false

# ============================================
# Stream Bot (Snapple Facts AI - stream.rig-city.com)
# ============================================
# Database password (auto-injected into DATABASE_URL by docker-compose)
STREAMBOT_DB_PASSWORD=generate-random-password
STREAMBOT_SESSION_SECRET=generate-random-secret

# Stream Bot-specific OpenAI key (optional - uses OPENAI_API_KEY if empty)
STREAMBOT_OPENAI_API_KEY=
STREAMBOT_OPENAI_BASE_URL=https://api.openai.com/v1

# Application Settings (optional - defaults shown)
STREAMBOT_NODE_ENV=production
STREAMBOT_PORT=5000

# Twitch Integration (OPTIONAL - only needed if using Twitch)
# Get from: https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TWITCH_CHANNEL=

# ============================================
# VNC Remote Desktop (vnc.evindrake.net)
# ============================================
VNC_PASSWORD=generate-random-password
VNC_USER=evin
VNC_USER_PASSWORD=generate-random-password
NOVNC_URL=https://vnc.evindrake.net

# ============================================
# Plex Media Server
# ============================================
# Get claim token from: https://www.plex.tv/claim/
PLEX_CLAIM=claim-token-here

# ============================================
# Generate Random Secrets
# ============================================
# Use these commands to generate secure secrets:
#
# API Key:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
#
# Session Secret:
#   python3 -c 'import secrets; print(secrets.token_hex(32))'
#
# Database Password:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(16))'
