# ════════════════════════════════════════════════════════════════
# GitHub Actions Secrets Configuration Template
# ════════════════════════════════════════════════════════════════
# Add these secrets to your GitHub repository:
# Settings > Secrets and variables > Actions > New repository secret

# ────────────────────────────────────────────────────────────────
# Production Server SSH Access
# ────────────────────────────────────────────────────────────────
PROD_SERVER_HOST={{ production_server_host }}
PROD_SERVER_USER={{ production_server_user }}
PROD_SSH_KEY={{ production_ssh_private_key }}

# ────────────────────────────────────────────────────────────────
# Staging Server SSH Access (Optional)
# ────────────────────────────────────────────────────────────────
STAGING_SERVER_HOST={{ staging_server_host }}
STAGING_SERVER_USER={{ staging_server_user }}
STAGING_SSH_KEY={{ staging_ssh_private_key }}

# ────────────────────────────────────────────────────────────────
# Docker Hub (Optional - for image registry)
# ────────────────────────────────────────────────────────────────
DOCKERHUB_USERNAME={{ dockerhub_username }}
DOCKERHUB_TOKEN={{ dockerhub_token }}

# ────────────────────────────────────────────────────────────────
# Notification Webhooks (Optional)
# ────────────────────────────────────────────────────────────────
DISCORD_WEBHOOK_URL={{ discord_webhook_url }}
SLACK_WEBHOOK_URL={{ slack_webhook_url }}

# ════════════════════════════════════════════════════════════════
# SETUP INSTRUCTIONS
# ════════════════════════════════════════════════════════════════
#
# 1. Generate SSH key pair for GitHub Actions:
#    ssh-keygen -t ed25519 -C "github-actions@homelab" -f github-actions-key
#
# 2. Add public key to production server:
#    ssh-copy-id -i github-actions-key.pub evin@your-server.com
#
# 3. Add private key to GitHub Secrets as PROD_SSH_KEY:
#    cat github-actions-key | pbcopy  # Copy private key
#    
# 4. Add server host and user to GitHub Secrets:
#    PROD_SERVER_HOST: your-server.com
#    PROD_SERVER_USER: evin
#
# 5. Test SSH connection from GitHub Actions:
#    ssh -i github-actions-key evin@your-server.com "echo 'Connection successful'"
#
# ════════════════════════════════════════════════════════════════
