# ═══════════════════════════════════════════════════════════════
# NEBULA COMMAND - PRODUCTION ENVIRONMENT TEMPLATE
# ═══════════════════════════════════════════════════════════════
# Copy to .env and fill in all values
# NEVER commit .env to git!

# ═══════════════════════════════════════════════════════════════
# SHARED (All Services)
# ═══════════════════════════════════════════════════════════════
POSTGRES_PASSWORD=<secure-password>
REDIS_URL=redis://redis:6379/0
SERVICE_AUTH_TOKEN=<generate: openssl rand -hex 32>
OPENAI_API_KEY=<your-openai-key>

# ═══════════════════════════════════════════════════════════════
# DASHBOARD (Flask)
# ═══════════════════════════════════════════════════════════════
FLASK_ENV=production
WEB_USERNAME=admin
WEB_PASSWORD=<secure-password>
JARVIS_DATABASE_URL=postgresql://jarvis:${JARVIS_DB_PASSWORD}@homelab-postgres:5432/homelab_jarvis
JARVIS_DB_PASSWORD=<secure-password>

# Plex Integration
PLEX_URL=http://100.64.0.1:32400
PLEX_TOKEN=<your-plex-token>

# Home Assistant Integration
HOME_ASSISTANT_URL=http://100.64.0.1:8123
HOME_ASSISTANT_TOKEN=<your-ha-token>
HOME_ASSISTANT_VERIFY_SSL=False

# DNS Management
CLOUDFLARE_API_TOKEN=<your-cf-token>
CLOUDFLARE_ZONE_ID=<your-zone-id>

# ═══════════════════════════════════════════════════════════════
# DISCORD BOT
# ═══════════════════════════════════════════════════════════════
DISCORD_BOT_TOKEN=<your-bot-token>
DISCORD_CLIENT_ID=<your-client-id>
DISCORD_CLIENT_SECRET=<your-client-secret>
DISCORD_DATABASE_URL=postgresql://discord_bot:${DISCORD_DB_PASSWORD}@homelab-postgres:5432/discord_bot
DISCORD_DB_PASSWORD=<secure-password>
SESSION_SECRET=<generate: openssl rand -hex 64>

# Twitch API (for stream notifications)
TWITCH_CLIENT_ID=<your-twitch-client-id>
TWITCH_CLIENT_SECRET=<your-twitch-client-secret>

# Cross-service webhook (MUST MATCH STREAM BOT)
STREAM_BOT_WEBHOOK_SECRET=<generate: openssl rand -hex 32>

# ═══════════════════════════════════════════════════════════════
# STREAM BOT
# ═══════════════════════════════════════════════════════════════
STREAMBOT_DATABASE_URL=postgresql://stream_bot:${STREAMBOT_DB_PASSWORD}@homelab-postgres:5432/stream_bot
STREAMBOT_DB_PASSWORD=<secure-password>
SESSION_SECRET=<generate: openssl rand -hex 64>
TOKEN_ENCRYPTION_KEY=<generate: openssl rand -hex 32>

# Twitch OAuth
TWITCH_CLIENT_ID=<your-twitch-client-id>
TWITCH_CLIENT_SECRET=<your-twitch-client-secret>
TWITCH_REDIRECT_URI=https://stream.rig-city.com/auth/twitch/callback

# YouTube OAuth
YOUTUBE_CLIENT_ID=<your-youtube-client-id>
YOUTUBE_CLIENT_SECRET=<your-youtube-client-secret>
YOUTUBE_REDIRECT_URI=https://stream.rig-city.com/auth/youtube/callback

# Kick (unofficial API)
KICK_CLIENT_ID=<optional>

# Spotify OAuth
SPOTIFY_CLIENT_ID=<your-spotify-client-id>
SPOTIFY_CLIENT_SECRET=<your-spotify-client-secret>
SPOTIFY_REDIRECT_URI=https://stream.rig-city.com/auth/spotify/callback

# Cross-service webhook (MUST MATCH DISCORD BOT)
DISCORD_BOT_URL=https://discord.rig-city.com
STREAM_BOT_WEBHOOK_SECRET=<same-as-discord-bot>

# ═══════════════════════════════════════════════════════════════
# PRODUCTION URLS
# ═══════════════════════════════════════════════════════════════
DASHBOARD_URL=https://dashboard.rig-city.com
DISCORD_BOT_URL=https://discord.rig-city.com
STREAM_BOT_URL=https://stream.rig-city.com
APP_URL=https://stream.rig-city.com

# ═══════════════════════════════════════════════════════════════
# INFRASTRUCTURE
# ═══════════════════════════════════════════════════════════════
# Tailscale IPs
LOCAL_UBUNTU_IP=100.110.227.25
LINODE_IP=100.66.61.51

# NAS
NAS_IP=192.168.0.198
NAS_MOUNT_PATH=/srv/media
