# ═══════════════════════════════════════════════════════════════
#  Nebula Command - Environment Configuration
#  
#  QUICK SETUP:
#    ./deploy.sh           # Auto-generates secrets & prompts for tokens
#    ./deploy.sh setup     # Interactive environment setup only
#    ./deploy.sh check     # Verify configuration
# ═══════════════════════════════════════════════════════════════

# ━━━ AUTO-GENERATED (deploy.sh handles these) ━━━
# These are automatically generated if missing - no action needed:
# POSTGRES_PASSWORD, DISCORD_DB_PASSWORD, STREAMBOT_DB_PASSWORD,
# JARVIS_DB_PASSWORD, SERVICE_AUTH_TOKEN, SESSION_SECRET, SECRET_KEY,
# REDIS_PASSWORD, JWT_SECRET

# ━━━ DISCORD BOT (REQUIRED) ━━━
# Discord bot token - REQUIRED for the bot to function
# Get from: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=

# Discord application credentials (optional, for OAuth features)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# ━━━ TAILSCALE (OPTIONAL - for local homelab) ━━━
# Only needed if connecting cloud services to local homelab
# Get from: https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTHKEY=

# ━━━ AI SERVICES (OPTIONAL) ━━━
# OpenAI API key for AI features (Jarvis, content generation)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ━━━ DNS MANAGEMENT (OPTIONAL) ━━━
# Cloudflare API token with DNS edit permissions
# Get from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=

# Your domain names (comma-separated)
# CLOUDFLARE_ZONES=example.com,mysite.net

# ━━━ STREAMING PLATFORMS (OPTIONAL) ━━━

# Twitch - https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=

# YouTube - https://console.cloud.google.com/apis/credentials
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=

# Spotify - https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Kick (if available)
KICK_CLIENT_ID=
KICK_CLIENT_SECRET=

# ━━━ LOCAL SERVICES (OPTIONAL) ━━━
# Tailscale IPs for local services (auto-discovered if using Tailscale)
# TAILSCALE_LOCAL_HOST=100.x.x.x
# TAILSCALE_LINODE_HOST=100.x.x.x

# Plex token for Now Playing features
# Get from: https://www.plex.tv/claim/
PLEX_TOKEN=

# Home Assistant token for automation features
# Get from: Home Assistant → Profile → Long-Lived Access Tokens
HOME_ASSISTANT_TOKEN=

# ━━━ DASHBOARD (OPTIONAL) ━━━
# Web login credentials (defaults to admin/admin if not set)
WEB_USERNAME=admin
WEB_PASSWORD=

# ═══════════════════════════════════════════════════════════════
#  NOTES:
#  - NEVER commit .env to git (only .env.example)
#  - Most internal secrets are auto-generated by deploy.sh
#  - Only DISCORD_BOT_TOKEN is strictly required
# ═══════════════════════════════════════════════════════════════
