# ═══════════════════════════════════════════════════════════════
#  Nebula Command - Local Ubuntu Environment Configuration
#  
#  QUICK SETUP:
#    ./deploy.sh           # Auto-generates secrets & deploys
#    ./deploy.sh setup     # Interactive environment setup only
#    ./deploy.sh check     # Verify configuration
# ═══════════════════════════════════════════════════════════════

# ━━━ AUTO-GENERATED (deploy.sh handles these) ━━━
# POSTGRES_PASSWORD, REDIS_PASSWORD, JWT_SECRET, SESSION_SECRET, etc.

# ━━━ PLEX MEDIA SERVER (OPTIONAL) ━━━
# Get token from: https://www.plex.tv/claim/
PLEX_TOKEN=
PLEX_CLAIM=claim-xxxxxxxxxxxxx

# ━━━ MINIO OBJECT STORAGE (OPTIONAL) ━━━
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=

# ━━━ HOME ASSISTANT (OPTIONAL) ━━━
# Generate: Profile -> Long-Lived Access Tokens -> Create Token
HOME_ASSISTANT_TOKEN=

# ━━━ CLOUDFLARE TUNNEL (OPTIONAL) ━━━
# Exposes local services without port forwarding
# Get token: cloudflared tunnel token my-tunnel
CLOUDFLARE_TUNNEL_TOKEN=
CLOUDFLARE_TUNNEL_ID=

# ━━━ STORAGE MONITORING (OPTIONAL) ━━━
# Enable ZFS pool monitoring (if using ZFS)
ZFS_ENABLED=false

# Discord webhook for storage alerts
# Create at: Discord Server Settings > Integrations > Webhooks
STORAGE_ALERT_DISCORD_WEBHOOK=

# Health check interval in seconds (default: 1 hour)
STORAGE_CHECK_INTERVAL=3600

# ━━━ WIREGUARD VPN (OPTIONAL) ━━━
# For secure mesh networking between servers
WIREGUARD_IP=10.200.0.2/24
WIREGUARD_PEER_IP=10.200.0.1

# ━━━ SUNSHINE GAMESTREAM (OPTIONAL) ━━━
# Start with: docker compose --profile gamestream up -d
SUNSHINE_PASS=

# Windows VM IP (KVM/libvirt)
WINDOWS_VM_IP=192.168.122.250

# ━━━ VNC DESKTOP (OPTIONAL) ━━━
# Start with: docker compose --profile vnc up -d
VNC_PASSWORD=

# ━━━ NAS ACCESS (OPTIONAL) ━━━
NAS_HOST=192.168.1.x
NAS_USER=admin
NAS_PASSWORD=

# ━━━ TIMEZONE ━━━
TZ=America/New_York

# ═══════════════════════════════════════════════════════════════
#  NOTES:
#  - This is for local Ubuntu homelab deployment
#  - Most services auto-configure with defaults
#  - NAS mount is optional but recommended for Plex media
# ═══════════════════════════════════════════════════════════════
