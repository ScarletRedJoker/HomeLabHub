# Production Environment Configuration
# Copy this file to .env and fill in your actual values

# Dashboard Security
DASHBOARD_API_KEY=generate-with-openssl-rand-hex-32
SESSION_SECRET=generate-with-openssl-rand-hex-32

# OpenAI API (for AI features)
OPENAI_API_KEY=sk-proj-your-key-here

# Let's Encrypt Email (for certbot SSL certificates)
LETSENCRYPT_EMAIL=admin@evindrake.net

# Discord Bot Configuration
DISCORD_BOT_IMAGE=ghcr.io/yourrepo/discord-bot:latest
DISCORD_TOKEN=your-discord-bot-token
BOT_PREFIX=!

# Plex Configuration
PLEX_CLAIM=claim-token-from-plex-tv

# Remote Desktop (optional - for noVNC or Guacamole connection)
# Leave empty if not using remote desktop features
# Example: http://localhost:6080/vnc.html
NOVNC_URL=

# Service Profiles (comma-separated: discord,plex,n8n,web)
# Start with just dashboard, then add services as you configure them
# Examples:
#   COMPOSE_PROFILES=plex,n8n,web  (start Plex, n8n, and static site)
#   COMPOSE_PROFILES=all           (start everything - requires Discord bot configured)
COMPOSE_PROFILES=plex,n8n,web
